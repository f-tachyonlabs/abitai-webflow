{
  "scripts": {
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "node --test tests/chat-helpers.test.js",
    "test:integration": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
